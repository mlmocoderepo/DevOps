name: My cool workflow

on:
  push:
  pull_request:
  workflow_dispatch:
  
jobs:
    myfirstjob:
              name: First job
              runs-on: ubuntu-latest
              steps:
              - name: Step One
                uses: actions/checkout@v3
              - name: Step Two
                run: |
                  touch newfile.yaml
                  echo "Please Alex, it is Friday. Let me escape." >> newfile.yaml
                  cat newfile.yaml
                  pwd
    secondjob:
              needs: [myfirstjob]
              name: Second Job
              runs-on: macos-latest
              steps:
              - name: Step One
                run: date      
    thirdjob:
              name: Third Job
              runs-on: windows-latest
              steps:
              - name: Step One
                run: date
                
                
          
